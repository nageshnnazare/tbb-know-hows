# Compiled example binaries (keep sources + Makefile)
examples/*
!examples/*.cpp
!examples/*.broken
!examples/Makefile

# Locally fetched build script (CI downloads it fresh)
htmler.sh

# Python
__pycache__/
*.pyc
